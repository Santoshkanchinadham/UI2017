ids can be targeted using # . this is unique

# -> id
. -> class
class attribute is used to share the same value across more than one elements/tags

BOX MODEL in css:
it creates a virtual "rectangular box" around the element which can be manipulated.

border-style
border-width
border-color

border: 2px(width) solid(style) #ff0000(color);

margin-left
margin-right
margin-top
margin-bottom
 
margin: 5px 2px 10px 60px;

standardization was introduced to box model and we have to follow this.

margin: <top> <right> <bottom> <left>
padding: <top> <right> <bottom> <left>
border: <left> <right> <bottom> <style> <color>

margin: 50px 30px 50px 30px; this is equivalent to margin: 50px 30px;

border-radius: 5px 5px 5px 5px;
<top-left> <top-right> <bottom-left> <bottom-right>
